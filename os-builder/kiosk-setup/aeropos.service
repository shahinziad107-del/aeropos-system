[Unit]
Description=AeroPOS Cashier Backend Server
After=network.target

[Service]
Type=simple
User=kiosk
WorkingDirectory=/opt/aeropos
ExecStart=/usr/bin/node server.js
Restart=always
RestartSec=3
Environment=PORT=3000
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
